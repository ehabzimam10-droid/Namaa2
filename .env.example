# Supabase project credentials (public anon key is safe to expose to clients,
# but keep it here as a template — never commit the real .env file)
VITE_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR_ANON_KEY
