# 📋 تقرير تصليح مشاكل تطبيق نماء (Fix Problems)

> **الهدف من هذا الإصلاح:** التطبيق ديمو تُعرض به الفكرة على زوار حقيقيين، لذلك ركّزنا على ثلاث أولويات:
> 1. **عزل بيانات كل زائر** حتى لا يعبث زائر بديمو زائر آخر.
> 2. **حماية المسارات** حتى لا يدخل أحد لوحات غيره من الرابط مباشرة.
> 3. **إزالة الهشاشة** التي قد تُظهر شاشة معطوبة لحظة عرض الفكرة.
>
> ✅ الحالة النهائية: `npm run lint` (0 أخطاء) + `npm run build` (نجاح كامل).
> ⛔ لم يتم رفع أي شيء إلى GitHub — كل التعديلات محلية.

---

## 1) المشاكل الجوهرية التي عولجت

### 1.1 قاعدة بيانات مشتركة بين كل الزائرين (أخطر مشكلة)
**المشكلة:** كل عملية (تحويل، إيداع، مهمة، دوري، إشعار، دردشة) كانت تُكتب في نفس قاعدة Supabase المشتركة. أي زائر كان يعبث بأرصدة "سالم" و"خالد" فيشاهد التالي ديمو مخرباً، وكان بالإمكان قراءة محادثات الآخرين في المستشار الذكي.

**الحل — نموذج الـ Sandbox المعزول:**
- أُعيدت كتابة `src/context/AppContext.tsx` بالكامل ليصبح مصدر البيانات هو **متصفح الزائر نفسه** (localStorage بإصدار جديد `namaa_sandbox_v15_*`) مزروعاً من البيانات الأصلية في `mockData.ts`.
- **أُزيلت كل استدعاءات Supabase من تدفقات البيانات** (الأرصدة، المهام، الحصالات، المشاريع، الدوري، الإشعارات، الدردشة، أرشيف الدوريات). بقيت Supabase فقط لتسجيل الدخول الاختياري بالبريد — وهذا مقصود.
- النتيجة: كل زائر يحصل على عائلة تجريبية خاصة به كاملة، ولا يمكن لأي أحد إفسادها أو رؤيتها.

### 1.2 لا يوجد زر Reset للديمو
**المشكلة:** بعد أول موجة تجارب تبقى البيانات وسخة إلى الأبد (خصوصاً مع القاعدة المشتركة السابقة).

**الحل:**
- دالة `resetDemo()` جديدة في السياق تمسح كل التعديلات وتعيد زرع البيانات الأصلية فوراً.
- زر 🔄 بجانب "تسجيل الخروج" في الشريط الجانبي (لكل الأدوار) مع رسالة تأكيد.
- قسم كامل "إعادة تعيين الديمو 🔄" في لوحة المطور (`DeveloperDashboard.tsx`) يشرح آلية العزل قبل الضغط.

### 1.3 المسارات غير محمية
**المشكلة:** أي شخص يفتح `/father` أو `/kid` أو `/dev` مباشرة من الرابط يدخل بدون تسجيل دخول، ويقدر يمسح مهام أو يوقف دوري شخص آخر أثناء العرض.

**الحل في `src/App.tsx`:**
- مكوّن حارس `RequireRole` يمنع الوصول إلا بحساب الدور الصحيح:
  - `/father/*` → للأب فقط.
  - `/kid/*` → للابن فقط.
  - `/dev` → للمطور فقط.
- من ليس لديه جلسة → يُعاد للصفحة الرئيسية، ومن يحاول دخول لوحة غير دوره → يُعاد للوحته.
- أي مسار غير معروف (`*`) → يعود لصفحة الدخول بدل صفحة بيضاء.
- إصلاح مرافق: زر "God Mode" في صفحة الدخول كان ينقل إلى `/dev` **بدون** تعيين حساب المطور، فسيتمرده الحارس الجديد — الآن يعيّن الدور ثم ينقل.

### 1.4 هشاشة منطق النقاط والمطابقة بالاسم
**المشكلة:** حساب نقاط الدوري كان مكتوباً **مرتين متكررتين** (في `calculateKidScores` وفي `endFamilyLeague`)، ويعتمد على مطابقة نص عنوان العملية مثل `title.includes('تبرع')` — أي تعديل حرف في النص يكسر النقاط. كما كانت مطابقة الأبناء أحياناً بالـ id وأحياناً بالاسم.

**الحل:**
- ملف `src/utils/appHelpers.ts` جديد يحتوي:
  - `LEAGUE_GOAL_TITLE` ثابت واحد بدل النص السحري المتناثر في 6 مواضع.
  - **محرك تنقيط موحّد** `computeLeagueScores()` مكتوب مرة واحدة ويستخدمه العرض الحي وإقفال الدوري معاً.
  - **نظام تصنيفات ثابت للمعاملات** `TxCategory` (allowance / reward / savings / investment / donation / purchase / other) يُضاف لحظة إنشاء كل عملية، مع `inferTxCategory()` كبديل تلقائي للبيانات القديمة — فلم تعد النقاط تعتمد على نصوص حرّة.
  - `resolveActiveKid()`: حل موحد لهوية الابن النشط (استبدل 8 نسخ مكررة من منطق `|| kids.find(k => k.name === 'سالم') || kids[0]` المنتشر في Topbar وكل صفحات الابن).

---

## 2) ميزة البحث — بناء كامل وتشغيل بأفضل طريقة

البحث **لم يكن موجوداً أصلاً** في التطبيق (كانت فقط فلاتر تبويبات)، فبُني من الصفر:

### المكونات الجديدة
| الملف | الوظيفة |
|---|---|
| `src/utils/searchUtils.ts` | محرك مطابقة عربي: تطبيع التشكيل والتطويل، توحيد همزات الألف (أ إ آ ٱ→ا)، التاء المربوطة والألف المقصورة، مع خريطة فهارس تسمح بتظليل الجزء المطابق داخل النص الأصلي |
| `src/components/ui/GlobalSearch.tsx` | نافذة بحث من نوع Command Palette |

### الخصائص
- **متاح للأب والابن معاً**: زر بارز في الشريط العلوي `Topbar` يظهر دائماً — أيقونة واضحة على الجوال، وحبة بحث بنص "ابحث في نماء..." وتلميح `Ctrl+K` على الشاشات الأكبر.
- **اختصار لوحة مفاتيح**: `Ctrl+K` / `⌘+K` يفتح البحث من أي مكان.
- **بحث واعٍ بالدور**:
  - **الأب**: الصفحات، الأبناء (مع الرصيد والمدخرات)، مهام كل ابن، المشاريع الاستثمارية، حالة الدوري، الإشعارات.
  - **الابن**: الصفحات، مهامه، حصالاته ونسب تقدمها، عملياته المالية، المشاريع، مبادرات التبرع، إشعاراته.
- **نتائج مرتبة ومجمّعة** بمجموعات (الصفحات، الأبناء، المهام...) مع ترجيح: تطابق البداية > احتواء متصل > تطابق كل الكلمات، وترجيح إضافي للعنوان على الوصف والكلمات المفتاحية.
- **تظليل المطابقة** داخل النتيجة (يشمل حال وجود تشكيل في النص الأصلي).
- **تنقل كبلوحة مفاتيح**: أسهم ↑↓ للتنقل، Enter للفتح، Esc للإغلاق، مع تمرير تلقائي للعنصر النشط.
- **وضع الجوال**: النافذة تظهر كـ Bottom Sheet بعرض الشاشة الكامل، وزر البحث أيقونة كبيرة واضحة بجانب زر القائمة والإشعارات.
- **حالة فراغ ذكية**: عند عدم كتابة شيء تظهر الصفحات كمشغّل تنقل سريع، وعند عدم وجود نتائج تظهر رسالة إرشادية.
- **النتيجة تنقلك للمسار الصحيح** (مثلاً البحث عن "دراجة" يفتح `/kid/savings`).

---

## 3) الأخطاء الإضافية المكتشفة أثناء الفحص الشامل والمصلَحة

| # | الخطأ | الموقع | الإصلاح |
|---|---|---|---|
| 1 | فلتر الشهر مثبت على **14 يوليو 2026** بشكل صلب — تحليل الشهر الحالي كان فارغاً دائماً | `FatherKidsPage.tsx:19` | يستخدم `new Date()` (الشهر الحالي فعلياً) |
| 2 | **ازدواج تسجيل العمليات**: استحقاق الحصالة كان يضيف العملية محلياً ثم يسجلها مرة ثانية | `AppContext` (checkSavingsStatus) | عملية واحدة فقط لكل استحقاق |
| 3 | **آثار جانبية داخل setState updaters** (قراءة صاحب المهمة/الأبناء المتأثرين داخل الدالة) — غير آمنة مع React 19 StrictMode وقد تُنفذ مرتين أو متأخراً | `AppContext` (submitTaskProof, startFamilyLeague) | الحساب يتم قبل التحديث، والدوال صارت نقية |
| 4 | **تعديل مباشر لكائنات الحالة** (mutation): `startFamilyLeague/endFamilyLeague` كانت تغير balance وsavingsGoals على نفس المرجع | `AppContext` | تحديثات immutable بالكامل عبر نسخ جديدة |
| 5 | تكليف مهمة لابن غير موجود كان يرسل الإشعار **لطالب ثابت خاطئ** `'kid_salem'` | `AppContext` (assignManualTask) | لا إشعار إن لم يوجد الطفل |
| 6 | `JSON.parse(localStorage)` بدون حماية — ملف تخزين تالف = **شاشة بيضاء** أثناء العرض | `AppContext` | `safeJsonParse()` مع قيمة افتراضية في كل قراءة |
| 7 | أنواع animation مستخدمة بدون تعريف (`animate-fade-in`, `animate-slide-down`) — كانت بلا مفعول | `tailwind.config.js` | تعريف keyframes فعلي + نقطة توقف `xs: 420px` |
| 8 | `index.html`: `lang="en"` بدون RTL والعنوان "nama" | جذر المشروع | `lang="ar" dir="rtl"` + عنوان ووصف عربي + theme-color |
| 9 | **`.env` بمفاتيح Supabase الحقيقية متتبَّع في Git** وغير مستثنى | `.gitignore` | إضافة `.env` للـ gitignore + `git rm --cached .env` (محلياً فقط دون أي رفع) + إنشاء `.env.example` كقالب |
| 10 | **بناء المشروع كان معطوباً** — `tsc` يفشل بسبب `import React` غير مستخدم في 9 ملفات وخصائص غير معرفة | SVG components + صفحات القرية | حذف الاستيرادات الميتة + إضافة `family_castle_level?: number` لنوع UserProfile (كانت تُقرأ دون تصريح) |
| 11 | `new Date(selectedPastLeague.end_date)` يقبل قيمة undefined محتملة (خطأ TS) | `FatherLeaguePage.tsx:668` | حماية بقيمة بديلة |
| 12 | **الدردشة الذكية تسرّب خصوصية**: تاريخ المحادثات يُقرأ ويُكتب في جدول مشترك بين كل الزوار | `FatherAICoachPage.tsx` | دردشة معزولة محلياً، حذف 5 عمليات insert وجلب التاريخ |
| 13 | **الدوريات المنتهية تُجلب من القاعدة المشتركة** — زائر يشوف دوريات زوار آخرين | `FatherLeaguePage.tsx` | أرشيف محلي `pastLeagues` في السياق يسجل كل دوري ينتهي (بنفس شكل السجلات القديمة حتى لا تتأثر واجهة السجل) |
| 14 | مؤقت التوست معرف بـ `any` | `AppContext` | `ReturnType<typeof setTimeout>` |
| 15 | تسجيل الخروج لا يمحو ملف الجلسة من التخزين | `AppContext` (logout) | `removeItem` للمفاتيح الخاصة بالجلسة |
| 16 | زر "لوحة المطورين God Mode" ينقل دون تعيين هوية (سيطرده الحارس الجديد) | `LoginPage.tsx` | يعيّن profile dev ثم ينقل |
| 17 | تكرار منطق هوية الابن النشط في 8 مواضع بافتراضات مختلفة | صفحات الابن + Topbar + TransactionsModal | `resolveActiveKid()` موحد |
| 18 | زر "سجل العمليات" والاسم يتكسران على شاشات ضيقة جداً | `Topbar.tsx` | تحسين responsive (إخفاء تدريجي، nowrap للرصيد) |
| 19 | التوست كان معرّفاً داخل AuthLayout وخارج Routes معاً (تكرار محتمل) | `App.tsx` | نسخة عامة واحدة فوق كل التطبيق |

---

## 4) الملفات الجديدة
```
src/utils/appHelpers.ts          ← الثوابت + محرك التنقيط الموحد + أدوات الأمان
src/utils/searchUtils.ts         ← محرك البحث العربي (تطبيع + ترجيح + تظليل)
src/components/ui/GlobalSearch.tsx ← نافذة البحث الشاملة (Ctrl+K)
.env.example                     ← قالب متغيرات البيئة بدون أسرار
تصليح مشاكل.md                   ← هذا التقرير
```

## 5) الملفات المعدَّلة
```
src/context/AppContext.tsx        ← إعادة كتابة كاملة (Sandbox + resetDemo + pastLeagues + إصلاحات)
src/App.tsx                       ← حماية المسارات RequireRole
src/components/layout/Topbar.tsx  ← زر البحث + اختصار Ctrl+K + هوية موحدة
src/components/layout/Sidebar.tsx ← زر إعادة تعيين الديمو 🔄
src/components/ui/TransactionsModal.tsx ← هوية موحدة
src/data/mockData.ts              ← نظام TxCategory + وسم البيانات الأولية
src/pages/FatherAICoachPage.tsx   ← عزل الدردشة عن القاعدة المشتركة
src/pages/FatherLeaguePage.tsx    ← أرشيف الدوريات محلياً
src/pages/DeveloperDashboard.tsx  ← قسم إعادة التعيين
src/pages/LoginPage.tsx           ← إصلاح God Mode
src/pages/FatherKidsPage.tsx      ← إصلاح تاريخ الفلتر المثبت
src/pages/Kid{Dashboard,Savings,Tasks,Investments,Donations,League,Castle}Page.tsx ← هوية موحدة
index.html                        ← عربي + RTL
tailwind.config.js                ← xs breakpoint + animations مفقودة
.gitignore                        ← استثناء .env
```
(بالإضافة لحذف `import React` الميت من 9 ملفات كانت تكسر `tsc`.)

---

## 6) ما بقي عمداً بدون تغيير
- **`supabase.auth` في صفحة الدخول ولوحة المطور**: بقيت كمسار اختياري للدخول الحقيقي؛ فشلها لا يؤثر على الديمو (يوجد fallback).
- **مجلد `Namaa2.git/` وملف `ezgif-*.zip`**: مخلفات خارج الكود لم أحذفها لأن الحذف قرار ملكك — يُنصح بنقلهما خارج المشروع.
- **حجم الحزمة (~720KB)**: تحذير build غير مانع؛ العلاج الصحيح مستقبلاً هو Code Splitting عبر `import()` ديناميكي — تأجيله مقصود لأنه ليس ضرورياً لمرحلة الديمو.
- **إعادة الهيكلة الكبرى للـ Context**: تفكيكه لعدة ملفات تحسين معماري يستحق وقته **بعد** إثبات الفكرة، وليس الآن — الكود الحالي صحيح ويعمل، فقط مركزي.

---

## 7) كيف تجرّب النتيجة؟
```bash
npm run dev      # تشغيل محلي
npm run lint     # الفحص: 0 أخطاء
npm run build    # البناء: ناجح
```
سيناريو عرض مقترح: افتح الموقع → "دخول كولي أمر" → جرّب بحرية → اضغط 🔄 في الشريط الجانبي قبل عرض الزائر التالي، أو افتح متصفحاً آخر وسيحصل تلقائياً على عائلة نظيفة خاصة به.
